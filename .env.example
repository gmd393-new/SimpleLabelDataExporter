# Development Environment Configuration
# Copy this file to .env for local development

# Database Configuration
# For local development with PostgreSQL (Docker):
# 1. Copy .env.docker.example to .env.docker and set a secure password
# 2. Run `docker compose up -d` to start the PostgreSQL database
# 3. Update the password below to match your .env.docker POSTGRES_PASSWORD
DATABASE_URL="postgresql://labelexporter:your_secure_password@localhost:5432/labelexporter_dev"

# Shopify Configuration (automatically set by `shopify app dev`)
# These values are typically loaded from shopify.app.toml and Shopify Partners
# You don't need to set these manually when using `shopify app dev`
# SHOPIFY_API_KEY=your-api-key-here
# SHOPIFY_API_SECRET=your-api-secret-here

# Environment
NODE_ENV=development
